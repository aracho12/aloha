# aloha
aloha!

`pip install monty`  
`pip install mendeleev`  
`git clone https://github.com/aracho12/aloha.git`  


in Jupyter notebook
`from aloha.cohpplot import * `

in Terminal
`cohp --help / -h`
